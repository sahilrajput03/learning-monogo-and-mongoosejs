use mydb
db.datingLikes.drop() // true
db.createCollection('datingLikes')
cls

db.datingLikes.insertMany([
	{name: 'usr1'},
	{name: 'usr2'},
	{name: 'usr3'},
	{name: 'usr4'},
	{name: 'usr5'}
])

db.datingLikes.find()

// db.datingLikes.find( {
//    $and: [
//       { x: 1 },
//       { $expr: { $eq: [ { $divide: [ 1, "$x" ] }, 3 ] } }
//    ]
// } )